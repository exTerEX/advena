nextflow_pipeline {

    name "Test pipeline default execution"
    script "../main.nf"
    profile "test"

    test("Should run with stub") {
        options "-stub-run"

        when {
            params {
                outdir = "$outputDir"
            }
        }

        then {
            assert workflow.success
        }
    }
}
